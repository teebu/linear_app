name: 'Linear App'
description: 'Create a comment in a linear ticket'
inputs:
  message:  # ref head of pull request
    description: 'message to create'
    required: true
    default: 'Hello'
outputs:
  time: # id of output
    description: 'The time we created comment'
runs:
  using: 'node12'
  main: 'app.js'